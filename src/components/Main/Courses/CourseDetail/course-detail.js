import React from 'react';
import { StyleSheet, Text, View, ScrollView } from 'react-native';

export default (props) => {
    return <View><Text>This is course detail</Text></View>
}

const styles = StyleSheet.create({

})